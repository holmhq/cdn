{
  "name": "flow-globe",
  "version": "0.0.2",
  "description": "Configurable animated geospatial flow map custom element. Lazy-loads amCharts, supports themed globe/map projections and custom Sankey flow data.",
  "type": "module",
  "main": "src/flow-globe.js",
  "module": "src/flow-globe.js",
  "files": ["src", "dist"],
  "scripts": {
    "build": "node build.js",
    "serve": "python3 -m http.server 8090"
  },
  "keywords": ["map", "globe", "sankey", "flows", "custom-element", "web-component"],
  "license": "MIT",
  "devDependencies": {
    "terser": "^5.31.0"
  }
}
