{
  "name": "pulse-faces",
  "version": "0.0.1",
  "description": "Animated SVG sentiment faces as a vanilla custom element. Configurable faces, selection mode, labels, sizing, and CSS-variable theming.",
  "type": "module",
  "main": "src/pulse-faces.js",
  "module": "src/pulse-faces.js",
  "files": ["src", "dist"],
  "scripts": {
    "build": "node build.js",
    "serve": "python3 -m http.server 8090"
  },
  "keywords": ["emotions", "sentiment", "faces", "custom-element", "web-component"],
  "license": "MIT",
  "devDependencies": {
    "terser": "^5.31.0"
  }
}
